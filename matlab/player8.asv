function [ decision ] = player8(K,op,turn)
%Tit for tat with Reconcilation
if (turn == 1)
    decision = 1; %cooperate in turn 1
else
    if (K(op,7,turn-1) == 1)
        decision = 1;
    else
        %check the recent history, if the uncooperative behaviour has
        %caused enough damage, that betraying and then reconciling is a bad
        %strategy, then reconcilation is offered.
        
    end
end

end